#include "monty.h"
/**
 * f_ten - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: does not return
*/
void f_ten(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
